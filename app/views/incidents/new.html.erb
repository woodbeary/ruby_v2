<%= turbo_frame_tag "modal" do %>
  <div class="fixed inset-0 bg-gray-500 bg-opacity-75 flex items-center justify-center">
    <div class="bg-white rounded-lg p-8 max-w-2xl w-full">
      <div class="flex justify-between items-center mb-4">
        <h2 class="text-2xl font-bold">New Incident</h2>
        <%= link_to "×", root_path, class: "text-gray-500 text-2xl font-bold hover:text-gray-700" %>
      </div>

      <%= form_with(model: @incident, class: "space-y-4") do |f| %>
        <div>
          <%= f.label :title, class: "block text-sm font-medium text-gray-700" %>
          <%= f.text_field :title, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
        </div>

        <div>
          <%= f.label :description, class: "block text-sm font-medium text-gray-700" %>
          <%= f.text_area :description, rows: 4, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
        </div>

        <div>
          <%= f.label :priority, "Priority Level", class: "block text-sm font-medium text-gray-700" %>
          <%= f.select :priority, 
              [
                ['P1 - Critical (System Down)', 'P1'],
                ['P2 - High (Major Impact)', 'P2'],
                ['P3 - Medium (Limited Impact)', 'P3'],
                ['P4 - Low (Minor Issue)', 'P4']
              ],
              {},
              class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
          <p class="mt-1 text-sm text-gray-500">
            Select the initial priority. Our AI will analyze and may suggest a different level.
          </p>
        </div>

        <div class="flex justify-end space-x-2">
          <%= link_to "Cancel", root_path, class: "bg-white py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
          <%= f.submit "Create Incident", class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
        </div>
      <% end %>
    </div>
  </div>
<% end %> 